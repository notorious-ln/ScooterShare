<UserControl x:Class="WpfMaterialControls.UsersView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1000">
    <Border Background="#F8F8FA" Padding="18">
        <Border.Resources>
            <Style x:Key="UsersSummaryCardStyle" TargetType="Border">
                <Setter Property="Background" Value="White" />
                <Setter Property="BorderBrush" Value="#ECECF3" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="18" />
                <Setter Property="Padding" Value="22,18" />
            </Style>

            <Style x:Key="UserListItemBorderStyle" TargetType="Border">
                <Setter Property="Background" Value="White" />
                <Setter Property="BorderBrush" Value="#E9EAF0" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="18" />
                <Setter Property="Padding" Value="18,16" />
                <Setter Property="Margin" Value="0,0,0,12" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#F8F9FD" />
                        <Setter Property="BorderBrush" Value="#E3E6F0" />
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect BlurRadius="12"
                                                  ShadowDepth="2"
                                                  Opacity="0.12"
                                                  Color="#000000" />
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Resources>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="300"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Управление пользователями"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="#222222"
                           VerticalAlignment="Center"/>
                <Border Grid.Column="1"
                        HorizontalAlignment="Right"
                        Background="#F3F4F7"
                        BorderBrush="#E8E9EF"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="14,8">
                    <Grid Width="240">
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 FontSize="14"
                                 Foreground="#666666" />
                        <TextBlock Text="Поиск пользователей..."
                                   Foreground="#B0B3BC"
                                   FontSize="14"
                                   IsHitTestVisible="False"
                                   VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SearchText}" Value="">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding SearchText}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="1" Margin="0,22,0,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Margin="0,0,12,0" Style="{StaticResource UsersSummaryCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Всего пользователей" Foreground="#9BA3AF" FontSize="13.5"/>
                        <TextBlock Text="{Binding TotalUsers}" FontSize="33" FontWeight="SemiBold" Margin="0,10,0,0" Foreground="#1F2430"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Margin="0,0,12,0" Style="{StaticResource UsersSummaryCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Активные" Foreground="#9BA3AF" FontSize="13.5"/>
                        <TextBlock Text="{Binding ActiveUsers}" FontSize="33" FontWeight="SemiBold" Margin="0,10,0,0" Foreground="#2EA35F"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource UsersSummaryCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Заблокированные" Foreground="#9BA3AF" FontSize="13.5"/>
                        <TextBlock Text="{Binding BlockedUsers}" FontSize="33" FontWeight="SemiBold" Margin="0,10,0,0" Foreground="#CC4B62"/>
                    </StackPanel>
                </Border>
            </Grid>

            <ScrollViewer Grid.Row="2"
                          VerticalScrollBarVisibility="Hidden"
                          HorizontalScrollBarVisibility="Disabled"
                          PanningMode="VerticalOnly">
                <ItemsControl ItemsSource="{Binding FilteredItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource UserListItemBorderStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="56"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="220"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Width="42" Height="42" Background="#ECECEE" CornerRadius="21" VerticalAlignment="Top">
                                        <TextBlock Text="{Binding Initials}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#555" FontWeight="SemiBold"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" Margin="12,0,16,0">
                                        <TextBlock Text="{Binding FullName}" FontSize="22" FontWeight="SemiBold" Foreground="#2B2B2B"/>
                                        <Border Background="{Binding StatusBackground}" CornerRadius="10" Padding="8,2" HorizontalAlignment="Left" Margin="0,6,0,8">
                                            <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusForeground}" FontSize="12"/>
                                        </Border>
                                        <TextBlock Text="{Binding Email}" Foreground="#7A7A7A" FontSize="14"/>
                                        <TextBlock Text="{Binding JoinedText}" Foreground="#7A7A7A" FontSize="14" Margin="0,4,0,0"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" VerticalAlignment="Bottom">
                                        <TextBlock Text="{Binding Phone}" Foreground="#7A7A7A" FontSize="14"/>
                                        <TextBlock Text="{Binding RidesRevenueText}" Foreground="#7A7A7A" FontSize="14" Margin="0,4,0,0"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                                        <Button Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding Id}" BorderThickness="0" Background="Transparent" ToolTip="Редактировать" Cursor="Hand">✎</Button>
                                        <Button Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding Id}" Margin="8,0,0,0" BorderThickness="0" Background="Transparent" Foreground="#CC4B62" ToolTip="Удалить" Cursor="Hand">⊘</Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
